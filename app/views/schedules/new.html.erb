<h1>イベント／講座スケジュール作成</h1>

<div class="row">
  <div class="col-md-6 offset-md-3">
    <%= form_with model: @schedule, url: schedules_path, local: true  do |f| %>
      <!-- %= f.date_field :date % --><br>
      <!-- %= f.select :area, ['関東', '関西', '九州'], {include_blank: false} % --><br>
      <!-- %= f.select :teacher, ['A', 'B', 'C'], {include_blank: false} % --><br>
      <%= f.text_area :details %><br>
      <%= f.file_field :image %><br>
      <%= f.submit %>
    <% end %>
  </div>
</div>